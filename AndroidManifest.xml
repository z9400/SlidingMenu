<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.z9400.test"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" 
            android:theme="@style/ActivityTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Activity1" android:theme="@style/ActivityTheme" />
        <activity android:name=".activity.Activity2" android:theme="@style/ActivityTheme" />
        <activity android:name=".activity.Activity3" android:theme="@style/ActivityTheme" />
        <activity android:name=".activity.Activity4" android:theme="@style/ActivityTheme" />
        <activity android:name=".activity.Activity5" android:theme="@style/ActivityTheme" />
        
    </application>

</manifest>
